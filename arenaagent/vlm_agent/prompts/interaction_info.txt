当前任务是"{self.task_text}"
当前任务提示（如有）:"{self.task_prompt}"
NPC回复（如有）:"{self.npc_reply}"
NPC任务提示（如有）:{self.npc_subject}

当前视野物体信息如下：
其中 object_id 是组合图右侧标注的映射 ID，也是物体动作唯一允许使用的 ID。
[
  {self.visiable_objects_info}
]

上一轮执行状态如下：
- 动作执行结果: {self.action_res}
- 任务结果: {self.apply_resp}

在此之前，你已经执行了以下动作：
[
  {self.action_histories}
]
